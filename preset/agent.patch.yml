# dsh-my-go agent preset — Sisyphus orchestration (agent-plane composition).
#
# 0.1.7 声明行范式：预设 = `@deepseek-ai/dsh-agent-preset` 的一枚 Loader 行，由本包
# bundle patch 声明（package.json dsh.bundle.patch 的第二枚）。旧的目录范式
# （$DSH_HOME/.agent-presets/dsh-my-go/ 下的 preset.yml + agent.cordis.yml）在
# 0.1.7 已无任何代码读取，随本次迁移整体退役——没有安装拷贝，也就没有安装同步。
#
# 组合内容与原 agent.cordis.yml 逐字同源，只有两处随范式改变：
#   1. 行列表搬进声明的 config.plugins；
#   2. broker 行由 `./tools/broker.mjs`（相对预设目录）改为包说明符
#      `dsh-my-go/preset/tools/broker.mjs`。理由：声明行 config.plugins 里的相对
#      路径**不被 Loader 锚定**——dsh-app-boot 的 anchorInsertedPluginNames 只走
#      insert 行自身与 `group: true` 的数组 config，而本行是映射 config，相对路径
#      会落到 profile 目录上解析失败。官方指引的口径是「Resolve assets from
#      installed packages rather than a preset directory」，包侧由 package.json
#      exports 放出该子路径。
#
# 组网约束（与内置 standard 预设同款，未变）：
#  - service 行必须坐在带 `isolate` realm 的 group 里；
#  - 委派组（subagent 工具）解析 HOST 的 subagents 注册表，需要
#    `isolate: workflowEngine`（触达它的行同处一个 realm）；
#  - broker 是包内 mjs 行（自足预设，不依赖预设目录）。
#
# 面向模型的「编排指令」是系统提示段 `dsh-my-go:orchestration`，教 Sisyphus 四具
# 工具与 AGENTS.md 的 QA 规则；人设与编排段由 broker（preset/tools/broker.mjs）
# 按工种动态注册——Sisyphus 拿全量人设 + 编排规则，子代理只拿最小人设。
- insert:
    - id: preset-dsh-my-go
      name: '@deepseek-ai/dsh-agent-preset'
      config:
        id: dsh-my-go
        name: 'MyGO!!!!! 模式'
        description: 'My tasks, where to GO????? 参考了 oh-my-openagent 设计的 DSH 智能体编排。'
        order: 2
        plugins:
          - id: agent-instructions
            name: '@deepseek-ai/dsh-agent-instructions'
            config:
              maxBytes: 65536

          # ── shell ───────────────────────────────────────────────────────────────────
          - id: tool-bash
            name: '@deepseek-ai/dsh-tool-bash'
            disabled: !!js process.platform === 'win32'

          - id: tool-pwsh
            name: '@deepseek-ai/dsh-tool-pwsh'
            disabled: !!js process.platform !== 'win32'

          # ── filesystem ──────────────────────────────────────────────────────────────
          - id: tool-fs
            name: '@deepseek-ai/dsh-tool-fs'

          - id: tool-fs-search
            name: '@deepseek-ai/dsh-tool-fs-search'
            config:
              sampleOverCapGlobResults: false

          # ── background jobs / skills / goals / plan / compaction ────────────────────
          - id: tool-jobs
            name: '@deepseek-ai/dsh-tool-jobs'

          - id: skill-filesystem
            name: '@deepseek-ai/dsh-skill-filesystem'

          - id: tool-skill
            name: '@deepseek-ai/dsh-tool-skill'

          - id: tool-goal
            name: '@deepseek-ai/dsh-tool-goal'

          - id: planning
            name: cordis:group
            group: true
            isolate:
              planMode: true
            config:
              - id: plan-mode
                name: '@deepseek-ai/dsh-plan-mode'
                config:
                  section: |
                        You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

                        Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

                        The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

                        Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

                        Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

                        When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

          - id: compaction
            name: cordis:group
            group: true
            isolate:
              compaction: true
              toolResultPruner: true
            config:
              - id: compaction-basic
                name: '@deepseek-ai/dsh-compaction-basic'
              - id: command-compact
                name: '@deepseek-ai/dsh-command-compact'
              - id: tool-result-pruner
                name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
                config:
                  thresholdChars: 8192
                  headChars: 4096
                  tailChars: 1024

          # ── delegation (subagent tools + broker host) ───────────────────────────────
          # The subagents registry lives on the HOST; these rows resolve it. The broker
          # host plugin (local mjs) registers the orchestration tools and model binding
          # into this preset's scope, so both Sisyphus and its children see them
          # (children are restricted from go_work/continue/forward by the broker).
          - id: delegation
            name: cordis:group
            group: true
            isolate:
              workflowEngine: true
            config:
              - id: tool-subagent-control
                name: '@deepseek-ai/dsh-tool-subagent-control'

              - id: tool-subagent-list-agents
                name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

              - id: tool-subagent
                name: '@deepseek-ai/dsh-tool-subagent'
                config:
                  provider: spawn
                  toolName: subagent
                  backgroundMode: continuable

              - id: tool-subagent-fork
                name: '@deepseek-ai/dsh-tool-subagent'
                config:
                  provider: fork
                  toolName: subagent_fork
                  backgroundMode: continuable

              - id: workflow-ptc
                name: '@deepseek-ai/dsh-workflow-ptc'
                config:
                  provider: spawn

              - id: tool-workflow
                name: '@deepseek-ai/dsh-tool-workflow'

              - id: tool-ralph
                name: '@deepseek-ai/dsh-tool-ralph'
                config:
                  subagentProvider: spawn
                  maxRounds: 64

          # ── broker (orchestration tools + prompt section) ───────────────────────────
          # Deliberately OUTSIDE the delegation group: this row only CONSUMES the host
          # `tools` / `subagents` / `systemPrompt` registries (it publishes nothing), so
          # it needs no isolate realm — and its `systemPrompt.section()` must land in the
          # agent's scope, which an `isolate` realm would hide from the assembly.
          - id: broker
            name: 'dsh-my-go/preset/tools/broker.mjs'
            config:
              # 报告外部化部署面总闸（0.4.0 线一期 1.3~1.6）：**显式配置优先于代码默认**
              # （代码缺省 `?? true`，D5 默认开不动）。基线采集期曾显式 false 保持关——
              # metrics（config.metrics ?? true）随部署点亮采「外部化开启前」的对照基线，
              # 外部化不同时点亮。基线关窗后翻回 true，即点亮 report_submit/report_fetch
              # 与报告闸门（prompt 条款、收尾落板、report-gate 补发链）。同步 preset 会
              # 覆盖本行，改完记得保留本键。
              reportExternalization: true

              # 读平面并行池（0.4.0 线二期，D5/D7/D8/D9）：读平面 = explore/librarian，
              # 写平面（Hermes/Hephaestus/Prometheus/Oracle/Looker/Apelles/自定义角色）恒单线。
              # 1 = 关闭 = 全局单线现状；2~3 = 读平面并发上限（>3 钳制 3，非法值回落 1）。
              # 挂载期读一次，不做运行时切换。缺省（`?? 1`）= 关闭。
              # 装机口径：保守起步的 2 已扩到上限 3（2026-09-18 扩池批）。同步 preset 会
              # 覆盖本行，改完记得保留本键且值必须为 3——防回潮哨兵
              # test/report-clause.test.mjs 的 readPoolSize pin 站岗，被改回 2/1 即红。
              readPoolSize: 3

              # 接力链总闸（0.4.0 线三期，D21~D29）：开 → chain_start/chain_resolve 注册。
              # 依赖 reportExternalization=true（D23 fail-fast：总闸关则 chain_start 直接
              # 报错指路，绝不静默降级）。挂载期读一次，不做运行时切换。
              # 缺省（`?? false`）= 关。
              relayChains: true

          # ── remaining model-facing rows ─────────────────────────────────────────────
          - id: tool-ask-user
            name: '@deepseek-ai/dsh-tool-ask-user'

          - id: tool-todo
            name: '@deepseek-ai/dsh-tool-todo'
            config:
              allowParallelInProgress: true

          - id: tool-web
            name: '@deepseek-ai/dsh-tool-web'
            config:
              fetch: false
              searchTimeoutMs: 60000
